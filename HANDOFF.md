# Memoria LP 引き継ぎ

## 現在の構成（2026-09-14）

静的なHTML/CSS/JS。ビルド工程は不要。アプリ本体とは別リポジトリ。

- `index.html`：LP本文、実画面の紹介、ストア・法的リンク、SEOメタデータ。
- `assets/site.css`：基本コンポーネント → アニメーション → レスポンシブ → motion軽減の順。同じ条件のメディアクエリは一つに統合済み。
- `assets/site.js`：スクロール連動・画面切替・表示演出。JS無効、IntersectionObserver非対応、motion軽減でも本文と画像を読める。
- `assets/brand/memoria-wordmark.svg`：提供された文字ロゴ。`favicon.svg` はLPの星のあしらいを使用。
- `_config.yml`：GitHub PagesのJekyll配信から作業記録を除外。

## デザインの合意

- 2026-09-14、FVは「家と時間が主役」「関係性を決めすぎない」「キャラはいるけれどキャラを売る構図にはしない」へ変更。9/10の顔・表情を強く見せるFV方針を更新。アニメ調と暖かな部屋の世界観は維持。
- 夜の青、暖色の灯り、植物、木とニット、細い星の軌跡を使用。カラー変数は維持。
- FVはPCで左に本文、右に空いたソファ・灯り・本・カップ・猫、人物は本棚の奥に小さく配置。モバイルは画像の下に本文を配置し、主コピーとAIの説明までの距離を短縮。
- Cycleは周囲の夜色とつながる曇りガラス面。別の背景色へ急に切り替えない。
- ブランドシンボル検討の囲みは家・シェルターを意味する。検討資料はローカルへ移動したが、意味を失わせる変更は避ける。シンボル案はLPには採用していない。

## 現在使用する画像

| ファイル（assets/images/） | 内容・由来 |
|---|---|
| home-with-time.webp | 現行FV。家・灯り・空いたソファを主役に、奥で本を読むひとりと猫。内蔵imagegenで再構成、1536×1024 |
| voices-genkan.webp | Voices「おかえり。」。帰ってきた目線の夜の玄関（靴・上がり框・棚のランタン・ペンダント・足元の行灯・奥の部屋）。人物・猫・ソファなし。1536×1024 |
| little-moments-v2.webp | カップ・本・猫の生成イラスト。World のみで使用 |
| cta-window.webp | CTA。外から見た夜の集合住宅で、ひとつだけ灯った窓（横76%付近）。1536×1024 |
| genkan.webp | ホーム。朔の挨拶。ステータスバー整理・ジェスチャーバー化。840×1871 |
| album.webp | アルバム。全＋朔・灯・凪、48ページ、「読みかけの本の話」9/10。841×1871 |
| living.webp | リビング。朔と灯が会話、凪が窓辺、自室バーも3人。低解像度の元画像からAI再構成。840×1871 |
| calendar.webp | カレンダー。9/15打ち合わせへの返答を朔に、自室バーは3人。840×1871 |

スクショ4枚はリリース済み画面を元にしたAI編集（実機の撮り直しではない）。朔以外の2人（灯・凪）は `_local/generation/avatar-sheet-20260912.png` のオリジナル人物に紹介用の名前をつけたもので、正式なキャラ設定ではない。制作記録は `_local/generation/image-brief-20260914-*.md`。
| social-preview-home.jpg | 現行OGP/Twitter画像。新FVの家に揃えた1200×630のJPEG。内蔵imagegenで背景・文字を構成 |
| botanical-sprig.svg / star-trail.svg | 葉・星の装飾 |

画面はすでにリリース済みなので「開発中」の注記を削除。紹介文面を編集したことを機能セクションの冒頭に一度だけ表示している。AI編集画像は細部が元スクショと完全一致するとは保証しない。

## 2026-09-11 レビュー対応

1. **LP本文のコピー調整はユーザー担当**。今回の依頼では仕様の断定表現や見出し・説明文・CTAを変更していない。画像のaltは新しい内容に整合させた。
2. 旧生成スクショ4枚を提供画面ベースへ差し替え。表示の拡大トリミングを外し、元画像に合う縦横比に変更。
3. 未使用の旧FV・静物画像・ZIP・変更前HTML/CSSを削除。シンボル検討資料・生成ログは `_local/` へ移動し、`.gitignore` とJekyllの除外対象にした。HANDOFFも配信対象から除外。
4. CSSの末尾の上書き群を各コンポーネントと各メディアクエリへ統合。旧FV参照と未使用の画面注記・写真番号スタイルを除去。
5. canonical、og:url、OGP画像と寸法・alt、twitter:image、SVG faviconを追加。公開先URLは `https://hosimiya7.github.io/`。
6. スキップリンクは `#top` に変更し、mainをフォーカス可能にした。JS無効PCのfeatureは内容に応じた高さに変更。

## 作業物と公開範囲

- `_local/brand-exploration/`：元のシンボル案。必要な原案をローカルに維持。
- `_local/generation/`：生成プロンプト記録。
- `_local/` 全体はコミットしない。`.gitignore`だけで既存履歴が消えるわけではない。過去コミットに入った作業物は履歴には残る。
- 今回はローカル修正まで。mainへのマージ、本番公開、履歴の書き換えは行っていない。
- ユーザー提供の元スクショはDownloadsに保持している。

## 確認

- ブラウザのPC表示・スマホ幅で画面差し替えとレイアウトを確認。
- JSを一切読み込まない確認ページで本文・画像の表示と、featureの最小高さ制約が外れていることを確認。
- 元のLP本文（注記を除く）と外部リンク、カラー変数の一致を検証。
- ローカル画像・CSSの参照先、アンカー、メディアクエリの重複解消、JS構文、差分の空白を確認。
- 実機確認・Jekyllでの実配信・SNSクローラーの取得確認は未実施。画像URLは公開後に有効になる。

## 2026-09-12 SEO・OGP改善

- 検索用title・descriptionに「メモリア」「AIチャット」を明記。SNS用は既存のブランドコピーと用途説明を併用。
- OGP画像は1200×630。内蔵imagegenで背景を再構成し、既存SVGロゴと正確な日本語文字を重ねた。生成記録は `_local/generation/social-preview-20260912.md`。
- `robots.txt` とトップページ1件の `sitemap.xml` を追加。実際の更新日を偽らないためlastmodは省略。
- LP本文・ストア側の名称、サブタイトル、キーワード設定は変更していない。
- 公開前のHTTP確認では、公開トップはNotionへの転送ページで、OGP画像URLは404。公開後にトップのcanonical、画像HTTP 200、SNSカードを再確認する。

## 2026-09-14 FVの入口調整

- 主コピー「今日のことを、誰かに覚えていてもらえる。」は維持。補足は「話したことを覚え、ともに変わっていくAI」に変更。
- 家を主役にした新FVとSNSカードを追加。既存の夜の青・暖色・植物・猫・本を引き継ぎ、人物は奥で本を読むひとり。名前のある関係や視線の集中を作らない構図。
- モバイルの画像領域と上部余白を短縮。375px・320px幅で主コピーとAIの説明を最初の画面内に表示。
- 本文に先行して存在したLiving/Cycleの未コミット変更は保持。実画面の内容・ストアリンクは維持。
- PC 1440×900、375×812、320×740の表示と横はみ出しを確認。320pxはJSを読み込まない検証ページでも確認。動き軽減用CSSは専用ローカル検証ページで適用して確認（OS設定による切替試験ではない）。
- ローカル参照、JS構文、差分の空白チェックを実施。公開・実機・SNS再取得・流入への効果は未確認。
- 生成方式は内蔵imagegen。生成プロンプトと書き出し条件は `_local/generation/fv-home-20260914.md`。
- 機能紹介はアルバム → ホーム → リビング → カレンダーの順へ変更。PCの連動画像と表示番号も同じ順に変更。既存の各momentアンカーIDは維持。
- 未参照の旧FV・旧OGP画像、以前のHTML/CSSバックアップと検証ページ、役目を終えた画像差し替え・CSS移行スクリプトを削除。必要な生成記録・原案・参照チェックは保持。
- 会話例はPC 1440px・スマホ375px・320px（JSなし）で表示と横はみ出しを確認。機能紹介の画像順一致と、先頭アルバムからホームへの連動切替を確認。検証用HTMLは確認後に削除。

## 2026-09-14 Worldの静物画像を復元

- FV直後のWorldセクションを、一度会話例のHTMLモックアップに置換したが、FVの夜色・暖色の世界観からいきなりチャットUI調の無地カードへ落差が大きく、「情緒→証拠」という導線（World/Voicesで世界観、Featuresで実画面）がここで前倒しに崩れると判断し、元の静物画像（little-moments-v2.webp）+ figcaptionの構成に戻した。
- 併せて、会話例内の話者ラベルが「あなた」/「AI」という無名表記だった点も、Memoriaが個体名（あなたの子、朔など）で語る前提と矛盾していたため解消。
- `.memory-example`関連CSS（本文・レスポンシブ両方）は削除し、`.world-visual` / `.world-photo`のルールを復元。

## 2026-09-14 機能紹介の順番と改行

- 機能紹介をホーム → アルバム → リビング → カレンダーの順へ変更。PCの連動画像と表示番号も同じ順。各momentアンカーIDは維持。
  - 直前のVoicesが「おかえり。」で終わるため、最初の実画面は同じ言葉がある玄関で受ける（情緒→証拠の橋）。
  - アルバム先頭だと記憶保存ツールに見え、`aso_copy.md` で避けた日記・ライフログ棚の期待を呼ぶ。アルバムは「なぜ覚えているのか／自分で外せる」の答えとして2番目に置く。
- 改行：見出し・引用・リード・機能説明の本文・短い文言は `word-break:keep-all` で、句読点と `<wbr>` でだけ改行する（Safariでも有効）。句読点のない長い句には文節の切れ目に `<wbr>` を入れた。文言そのものは変更していない。
- 機能説明の本文は手動 `<br>` を削除（PCで「言います。」だけの行などが出ていた）。`word-break:auto-phrase` はChromeで「は／げましでも」と誤分割したため不採用。
- **文言を直すときの注意**：句読点のない10字以上の句には、文節の切れ目に `<wbr>` を入れる。入れないと、収まらない幅では任意の位置で折れる（`overflow-wrap:anywhere` の保険）。

## 2026-09-14 画像の差し替え

- 同じ部屋・小物の反復を解消。Voices背景を玄関（voices-genkan.webp）、CTA背景を外から見た灯りの窓（cta-window.webp）に変更。画面紹介の背後は写真をやめ、光のにじみのみ（無地になると「Memoria」の透かし文字が途切れて浮き出たため、透かしも削除）。little-moments-v2 は World だけに残した。旧 welcome-home.webp は削除。
- Voicesの灯りのにじみ位置を新しい光源に合わせた（PC 1440×900基準。ペンダント・棚のランタン・足元の行灯）。スマホは背景の切り抜きを横76%に寄せ、ペンダントと足元の行灯を画面内に収めた。
- CTAの画像は幅116%にして、灯った窓を中央のカードの右側に出した。スマホはほぼカードに隠れる。
- 差し替え直後は玄関・CTAとも暗く「ホラー感がある」との指摘。画像は変えず、CSSで明るさと暖色を上げ、玄関のぼかしを3px→1px、暗い幕を弱め、CTAの暗いグラデーションとカードの不透明度を下げた。3案（現状・中間・明るめ）をPC/スマホで比較し、明るめを採用。
- スクショ4枚はキャラを朔・灯・凪の3人に統一（依頼：`_local/image-brief-20260914.md`）。リビング・アルバムのwidth/height属性と、内容が変わったaltを更新。
- 01（ホーム）の引用を、ユーザー指示で「『おかえり。』から、昨日の話のつづきへ。」に変更（Astraの提案）。画面（名前なし・「おかえり。」）に合わせ、ユーザー判断で見出しを「帰ってくる場所がある」、本文を「アプリを開くと、あなたの子が迎えてくれます。話すほどに、関係も少しずつ変わっていきます。」に変更。

## 2026-09-14 ヘッダーの導線

- ヘッダーの「暮らしのなかで」を「できること」に変更（飛び先は Life with Memoria の `#features` のまま）。
- 不具合修正：Voices終盤のスクロールロック（2.2秒停止）が、ページ内リンクのスムーススクロール中にも発動し、ヘッダーの「暮らしのなかで」「はじめる」がおかえりセクションで止まっていた（PC・スマホとも。「はじめる」はダウンロード欄に届かなかった）。リンクのクリック後はスクロールが落ち着くまでロックしないようにした。自分でスクロールして読む場合のロックは従来どおり。
- 「できること」を押すと、減速中のスムーススクロールがおかえりの固定区間と重なり、一瞬止まってから次へ進むように見えていた（計測：固定区間で約0.3秒静止、その後0.6秒かけて減速。描画落ちはなし）。1.5画面以上離れたページ内リンクは、行き先の手前（画面の35%）まで瞬時に移動し、最後の短い距離だけ約0.4秒で滑らせるように変更。滑らせる部分はJSでフレームごとに位置を動かしている（瞬時の移動の直後にブラウザ標準のスムーススクロールを始めると、Chromeで上部が数フレーム空白になる描画ずれが出たため。対照実験で確認）。途中でホイール・タッチ・キー操作があれば止める。近いリンク・動き軽減・修飾キー付きクリックはブラウザ標準のまま。URLのハッシュは history.pushState で更新。
- ロック自体（site.js）は本作業以前からの未コミット変更。

## 固定リンク

- App Store: https://apps.apple.com/jp/app/id6776899625
- Google Play: https://play.google.com/store/apps/details?id=com.stelladear.memoria
- 利用規約: https://indecisive-helicona-37f.notion.site/36ae8ceaad54803abcbbd408b5c421f9
- プライバシーポリシー: https://indecisive-helicona-37f.notion.site/36ae8ceaad54803e91b1c64bda518132
- 特定商取引法: https://indecisive-helicona-37f.notion.site/36ae8ceaad548028ba85df03d50ce8ae

`app-ads.txt` はAdMob用なので変更しない。
